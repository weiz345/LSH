dataset:
  name: olivetti

model:
  type: toy
  embedding_dim: 64

embedding:
  cache_path: cache/random_embeddings.npy
  use_cache: true

similarity:
  source: raw     # "embedding" OR "raw"
  metric: cosine
  k: 5
  query_index: 0
